package test.com;

public class Test01Collection {

	public static void main(String[] args) {

		// map에 들어있는 모든 애들을 자바에서 자료구조라고 한다.
		// Collection<E>
		// E는 element이며 반드시 객체 타입이어야 한다.
		// Interface Map<K,V> 
		// K : key. 숫자, 문자. 대부분 문자로 쓴다
		// M : mapping, 오브젝트만 가능.
		
		// 자바의 자료구조
		// Collection을 상속받은 List(List를 상속받는 ArrayList, Vector),
		// Set(Set을 상속받는 HashSet)...
		// Map을 상속받은 HashMap, HashTable
		
		// 1.List(index)
		// 배열 처리를 함에 있어서 배열의 길이를 확정 짓지 않고
		// 데이터를 추가하는대로 길이가 정해진다.
		// 순서가 있으며, 중복된 데이터를 허용한다. >> index 존재
		// capacity가 정해져있긴 하지만 그게 길이는 아니다. 
		// 길이를 찍어보면 아무것도 안 넣으면 0이다.
		// 데이터를 넣고 빼는 것에 따라 계속 바뀐다.
		
		
		// 2.Set(next)	data 자체가 unique
		// 배열 처리를 함에 있어서 배열의 길이를 확정 짓지 않고
		// 데이터를 추가 하는대로 길이가 정해진다.
		// 순서가 없으며, 중복된 데이터를 허용하지 않는다.
		// 몇 번방이라는 개념이 없기 때문에 그냥 주머니에다 넣은 것 뿐이다.
		// 그래서 같은 게 있으면 안 된다.
		
		
		// 3.Map(key)	key는 반드시 unique해야 한다
		// 배열 처리를 함에 있어서 배열의 길이를 확정짓지 않고
		// 데이터를 추가하는대로 길이가 정해진다.
		// 순서의 개념이 아니고 키와 값의 개념으로 데이터 추가
		// 중복 허용
		// 다음 key 뭐야? 하고 key 값을 꺼낼 수 있다.
		// 키에 값 주는 게 mapping
		
		
		
		
		
		
		
		
	}

}
